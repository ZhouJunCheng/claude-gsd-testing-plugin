# 强制所有 shell 脚本使用 LF 行尾（防止 Windows git clone 转成 CRLF 导致 WSL 执行报错）
*.sh   text eol=lf
*.js   text eol=lf
*.md   text eol=lf
*.json text eol=lf
# Windows 批处理文件必须用 CRLF，否则 cmd.exe 可能行尾解析异常
*.bat  text eol=crlf
